name: kiosk
description: "free-kiosk ebook 3장 완료본"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.3.3
  path_provider: ^2.1.2
  crypto: ^3.0.6
  url_launcher: ^6.3.1
  go_router: ^14.6.2
  qr: ^3.0.2
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - data/config/store.json
    - data/config/tables.json
    - data/menu/menu_items.json
    - data/orders/active.json
    - data/history/order_archive.json
    - data/images_menu/
